"""DOCX parsing: order-preserving extraction into neutral structured data.

Named ``docx_parser`` (not ``docx``) so the package never shadows the
third-party ``docx`` (python-docx) module on import.
"""
